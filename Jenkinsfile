pipeline {
    agent any

    // triggers {
    //    githubPush()
    // }

    stages {
        stage('Hello, world!') {
            steps {
                echo 'I\'m actually working!!'
            }
        }
    }
}